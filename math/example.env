#!/bin/bash

# Useful if running math outside of docker.
# cp example.env .env, modify as needed

export DATABASE_IGNORE_SSL=true
export DATABASE_URL=DATABASE_URL=postgres://postgres:oiPorg3Nrz0yqDLE@localhost:5432/polis-dev
export DD_ENABLED=false
export LOGGING_LEVEL=debug
export MATH_ENV=dev
export MATH_ZID_ALLOWLIST=
export RECOMPUTE=true

# Optional math worker overrides. Defaults are safe for a 512 MB Heroku dyno.
# export MATH_JVM_PROFILE=heroku-512m
# export MATH_JVM_OPTS="-J-Xms64m -J-Xmx220m -J-Xss512k -J-XX:MaxMetaspaceSize=128m -J-XX:ReservedCodeCacheSize=48m -J-XX:MaxDirectMemorySize=32m -J-XX:+UseSerialGC -J-XX:ActiveProcessorCount=2 -J-XX:+ExitOnOutOfMemoryError"
# export MATH_WORKER_TIMEOUT_SECONDS=14400
# export MATH_WORKER_RESTART_DELAY_SECONDS=10
# export MATH_POLL_BATCH_SIZE=1000
# export MATH_CUTOFF_MAX_PTPTS=100000
# export MATH_CUTOFF_MAX_CMNTS=10000
